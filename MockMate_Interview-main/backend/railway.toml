[build]
builder = "NIXPACKS"
buildCommand = "mvn clean package -DskipTests"

[deploy]
startCommand = "sh -c 'java -jar $(ls target/*.jar | head -n 1)'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
